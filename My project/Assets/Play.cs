using UnityEngine;

public class Player:Monobehaviour
{private int numero:
public float velocidade * 40:    {
 void Start()      
numero=0:
    //Debug.Log(numero);
  //numero = numero + ;
  
  
  1f (Input.getKey(KeyCode.A))
  GameObject.transfora.position *= new Vector3(velocidade, 0, 0);  
  void Update()
 
      
  1f (Input.GetKey(KeyCode.D))
    {
        
    

